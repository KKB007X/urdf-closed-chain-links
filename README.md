# urdf-closed-chain-links
URDF cannot directly represent closed-chain mechanisms because a child link cannot have multiple parents. This project explores workaround methods for simulating closed-chain gripper mechanisms in Gazebo.
